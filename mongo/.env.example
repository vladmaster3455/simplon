# ============================================
# FICHIER DE CONFIGURATION - BACKEND API
# ============================================
# Copiez ce fichier en .env et modifiez les valeurs selon vos besoins

# Port du serveur Express
PORT=5000

# Base de données MongoDB
# Option 1: MongoDB Atlas (cloud)
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/database?retryWrites=true&w=majority

# Option 2: MongoDB local
# MONGODB_URI=mongodb://localhost:27017/mini_bank

# Clé secrète JWT (IMPORTANT: générez une clé unique et sécurisée)
JWT_SECRET=votre_cle_secrete_unique_a_changer_en_production

# Environnement
NODE_ENV=development

# Préfixe de l'API (toutes les routes commenceront par ce préfixe)
API_PREFIX=/serge

# Configuration de sécurité
TOKEN_EXPIRY=24h
BCRYPT_ROUNDS=12

# Configuration optionnelle pour la production
# CORS_ORIGIN=http://localhost:3000
# RATE_LIMIT_WINDOW_MS=900000
# RATE_LIMIT_MAX=100

# ============================================
# INSTRUCTIONS
# ============================================
# 1. Copiez ce fichier et renommez-le en .env
# 2. Modifiez les valeurs selon votre environnement
# 3. Ne commitez JAMAIS le fichier .env dans Git
# 4. Générez une clé JWT_SECRET unique avec: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
